{
  "name": "refinitiv-widget-build",
  "workspaces": [
    "packages/widgetA"
  ],
  "scripts": {
    "test": "vitest --run"
  },
  "devDependencies": {
    "prettier": "^2.8.8",
    "vitest": "^0.30.1"
  }
}